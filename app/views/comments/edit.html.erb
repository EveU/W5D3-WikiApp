<h2>Edit Comment for <%= @comment.article.title %></h2>

<%= form_for([@comment.article, @comment]) do |f| %>
  <p>
    <%= f.label :comment %>
    <%= f.text_area :comment %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %> 

<%= link_to 'Cancel', article_path(@comment.article) %>